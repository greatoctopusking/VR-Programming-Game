using UnityEngine;

/// <summary>
/// Tracks which catalog prefab this block instance belongs to (persists on and off the shelf).
/// </summary>
public class CodeBlockPoolItem : MonoBehaviour
{
    public GameObject sourcePrefab;
}
